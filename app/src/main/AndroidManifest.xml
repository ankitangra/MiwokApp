<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.multiscreens">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Miwok"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Family"
            android:label="Family"
            android:parentActivityName="com.example.admin.multiscreens.MainActivity"
            />
        <activity android:name=".Numbers"
            android:label="Numbers"
            android:parentActivityName="com.example.admin.multiscreens.MainActivity"
            />
        <activity android:name=".Phrases"
            android:label="Phrases"
            android:parentActivityName="com.example.admin.multiscreens.MainActivity"
            />
    </application>

</manifest>